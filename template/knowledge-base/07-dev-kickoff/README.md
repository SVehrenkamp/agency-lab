# 07 · Dev Kickoff

The first time architecture and system design enter the picture, drawing on every
prior artifact. Architecture Decision Records live in `adr/`.

- **Consumes:** all upstream artifacts
- **Produces:** `architecture.md`, `system-design.md`, `adr/*.md`
- **Default mode:** draft-then-approve (auto-draft → review)
- **Backing skills:** `engineering:architecture`, `engineering:system-design`
